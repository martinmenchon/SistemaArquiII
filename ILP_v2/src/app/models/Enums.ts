export enum FUType {
    ARITHMETIC, MEMORY, MULTIFUNCTION
}

export enum InstStatus {
    WAITING, IN_PROGRESS, DONE
}

export enum InstType {
    ADD = 'ADD',
    SUB = 'SUB',
    MUL = 'MUL',
    DIV = 'DIV',
    ST = 'ST',
    LD = 'LD'
}